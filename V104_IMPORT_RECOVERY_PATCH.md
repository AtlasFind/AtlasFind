# AtlasFind v1.0.4 Logo Import Recovery Patch

This patch adds safe alternate-candidate recovery, content sniffing for incorrectly
labelled image responses, non-ASCII URL encoding, and a robust error-list command.
It does not lower the minimum logo resolution and does not allow unsafe SVG files.
